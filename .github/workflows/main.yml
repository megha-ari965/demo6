name: Demo Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello Megha! GitHub Actions is working!"

      - name: Testing GitHub Actions Step
        run: echo "Testing GitHub Actions"
